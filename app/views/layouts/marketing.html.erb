<!DOCTYPE html>
<html lang="en">
  <head>
    <title><%= content_for?(:title) ? yield(:title) + " — Hyperbulletin" : "Hyperbulletin" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="description" content="<%= content_for?(:description) ? yield(:description) : "Send newsletters, without the nonsense of newsletter builders." %>">
    <link rel="icon" href="/favicon.png" type="image/x-icon">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag "tailwind", "data-turbo-track": "reload" %>
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body class="bg-stone-50 text-stone-800 font-sans antialiased">
    <% if notice = flash[:notice] || flash[:alert] %>
      <div class="flash" data-controller="element-removal" data-action="animationend->element-removal#remove">
        <div class="flex items-center justify-center px-4 py-2 m-auto mt-3 font-sans text-sm text-white rounded-md shadow-sm bg-stone-900 flash__inner">
          <%= notice %>
        </div>
      </div>
    <% end %>

    <nav class="mx-auto px-5 sm:px-0 mb-5 sm:mb-12 mt-12 flex sm:items-center justify-between max-w-xl">
      <%= link_to root_path, class: "font-serif font-medium text-stone-800 text-2xl" do %>
        Hyperbulletin
      <% end %>

      <div class="hidden sm:flex items-center text-xs font-sans rounded-xl shadow outline outline-stone-200/60 outline-1 bg-white p-1 text-stone-600">
        <%= link_to "Pricing", pricing_path, class: "hover:text-stone-800 py-1 px-3 duration-200 transition-colors" %>
        <%= link_to "Get Updates", updates_path, class: "hover:text-stone-800 py-1 px-3 duration-200 transition-colors" %>
        <%= link_to "About", about_path, class: "hover:text-stone-800 py-1 px-3 duration-200 transition-colors" %>
      </div>

      <div class="flex sm:hidden gap-2 text-xs font-sans text-stone-600">
        <%= link_to "Pricing", pricing_path, class: "py-1 px-2" %>
        <%= link_to "About", about_path, class: "py-1 px-2" %>
      </div>
    </nav>

    <%= yield %>

    <footer class="mx-auto px-5 sm:px-0 mt-20 pt-8 flex flex-col gap-4 justify-between relative border-t border-stone-300 max-w-xl">
      <div class="pb-8 space-y-2">
        <div class="grid grid-cols-5">
          <div class="uppercase tracking-widest font-medium text-stone-500 text-xs mb-5 col-span-3">
            As simple as e-mail
          </div>

          <nav class="text-stone-500">
            <div class="text-sm text-stone-800 font-medium mb-2">Product</div>
            <div><a href="https://github.com/alexisbouchez/hyperbulletin" class="hover:text-stone-800 text-sm duration-200 transition-colors">GitHub</a></div>
            <div><%= link_to "Pricing", pricing_path, class: "hover:text-stone-800 text-sm duration-200 transition-colors" %></div>
            <div><%= link_to "Updates", updates_path, class: "hover:text-stone-800 text-sm duration-200 transition-colors" %></div>
          </nav>

          <nav class="text-stone-500">
            <div class="text-sm text-stone-800 font-medium mb-2">Company</div>
            <div><%= link_to "About us", about_path, class: "hover:text-stone-800 text-sm duration-200 transition-colors" %></div>
            <div><%= link_to "Privacy", policy_privacy_path, class: "hover:text-stone-800 text-sm duration-200 transition-colors" %></div>
          </nav>
        </div>
      </div>
    </footer>
  </body>
</html>
